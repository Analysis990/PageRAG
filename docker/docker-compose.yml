services:
  backend:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: pagerag-backend
    ports:
      - "8000:8000"
    env_file:
      - ../.env
    environment:
      - QDRANT_URL=http://qdrant:6333
    volumes:
      - ../app:/app/app
      - ../data:/app/data
      - ../scripts:/app/scripts
      - ../lib:/app/lib
    depends_on:
      - qdrant
    networks:
      - pagerag-network

  qdrant:
    image: qdrant/qdrant:latest
    container_name: pagerag-qdrant
    ports:
      - "6333:6333"
    volumes:
      - ../data/qdrant_data:/qdrant/storage
    networks:
      - pagerag-network

networks:
  pagerag-network:
    driver: bridge
